import {IActionsConfig} from '../namespace';

const actionsConfig: IActionsConfig = {
  // ignore : 'игнорировать',
  block : 'запретить',
  permit : 'разрешить',
  // forceDuplex : 'двусторонняя печать',
  // forceGrayscale : 'черно-белая печать',
  // addWatermark : 'добавить метку',
  // addSign : 'подписать',
};

export { actionsConfig };
