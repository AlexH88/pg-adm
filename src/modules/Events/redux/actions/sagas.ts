// import { put, call, select, takeLatest, take, takeEvery } from 'redux-saga/effects';
import {SagaIterator} from 'redux-saga';
// import { startSubmit, stopSubmit, initialize, getFormValues } from 'redux-form';
// import {
//   getErrorMsg,
//   getResourceById,
//   IInjectedResource,
//   formatByResource,
//   checkFieldsCatalogForm,
//   } from 'shared/helpers';
import {IExtraArguments} from 'shared/types/app';
// import { IUser } from 'shared/types/users';
// import * as showResourceFeature from 'features/showResource';
// import { switchRemoveModalStatus, switchModalStatus } from './communication';
// import { deleteResource, eraseSelectedUsers, checkedUserMultiEdit } from './communication';
// import * as actionTypes from '../../namespace';
// import injectResource from 'shared/helpers/injectResource';
// import * as CryptoJS from 'crypto-js';
// import { getUserById, selectIsAllSelected } from 'modules/Users/redux/actions/selectors';

function getSaga({ api }: IExtraArguments) {

  if (!1) console.log(api);

  function* saga(): SagaIterator {

  }

  return saga;
}

export default getSaga;
