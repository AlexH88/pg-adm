import {ICatalogHeaderConfigs} from '../namespace';

const headerCatalogConfigs: ICatalogHeaderConfigs = {
  type: {
    ad: 'Active Directory',
    edir: 'edir',
  },
};

export { headerCatalogConfigs };
